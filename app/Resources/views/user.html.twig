<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Live & Learn</title>
</head>
<body>
{% for cat in cats %}
    <a href="/cats/{{ cat.slug }}">{{ cat.name  }}</a>

{% endfor %}
<hr>

посты
{{ user[0].name }}:

{% for post in posts %}
    <a href="/p/{{ post.id }}">{{ post.title }}</a>
    <a href="/p/{{ post.id }}">    <img src="{{ post.img }}" alt=""></a>

    <a href="/cats/{{ post.cat.slug }}">{{ post.cat.name }}</a>
    {{ post.description }}
    <br>
{% endfor %}



</body>
</html>